# 업무 처리 흐름 도식

- 정본과 표시: `전문가 에이전트 정의 2.md`의 「업무 처리 흐름」 안에 있는 **Mermaid 코드 블록 하나**다. 이 블록에서 모든 문구와 연결을 직접 수정한다.
- 사용자 요구: 이미지를 사용하지 않는다. 별도 도식 파일이나 생성기로 관리하지 않는다. 전체 흐름은 하나의 순서도로 이어져야 한다.
- **디자인은 이 문서의 다른 도식 44개와 똑같이 간다. 이 도식만의 색·도형·글자 크기·배경을 새로 만들지 않는다.**
- 머리글(`%%{init}%%`): 다른 도식과 같은 값을 쓴다 — `wrappingWidth: 640`, `curve: "basis"`, `nodeSpacing: 45`, `rankSpacing: 55`, `padding: 14`, `fontSize: "15px"`. 여기에 배치용 `layout: "elk"`와 elk 옵션만 덧붙인다(노드 125개라 dagre로는 배치가 무너진다). `theme`·`themeVariables` 색 지정·`themeCSS`·`linkStyle`은 두지 않는다.
- 도형과 색: 6가지 표준 반만 쓴다.
  | 뜻 | 도형 | 클래스 |
  |---|---|---|
  | 단계 표지 · 시작 · 종료 · 들어오는 신호 | `(["…"])` 캡슐 | `good` 초록 |
  | 판단 | `{{"…"}}` 육각형 | `note` 노랑 |
  | 행동 | `["…"]` 사각형 | `main` 파랑 |
  | 사람과 주고받기(요청·보고·되묻기) | `["…"]` 사각형 | `back` 주황 |
  | 기록 · 자료 | `[("…")]` 원통 | `store` 회색 |
  | 거절 · 중단 | `["…"]` 사각형 | `stop` 빨강 |
  마름모 `{"…"}`와 평행사변형 `[/"…"/]`은 쓰지 않는다. `classDef` 6줄은 다른 도식에서 그대로 복사해 온다.
- 배치: `flowchart TB`로 위에서 아래로 배치한다. 단계 구역은 같은 그래프 안의 `subgraph`이며, 분기·복귀선은 실제 목적지에 직접 연결한다.
- subgraph 배경: 배경을 채우지 않는다. 구역마다 `style PHASE_n fill:none,stroke:#94a3b8,stroke-width:1px,stroke-dasharray:5 4,color:#64748b`를 적는다(다른 도식의 subgraph와 같은 처리). 이 줄이 없으면 Mermaid 기본 노란 배경이 깔려 노란 판단 육각형과 섞인다.
- 로직: 승인 답변 도착과 실제 승인 여부를 구별한다. 반려·보류는 기록·보고로, 다음 신호는 기존 건 불러오기로 연결한다. 네 갈래 수정 원인은 순차 판단으로 배치한다.
- 검증: Mermaid 11.17에서 코드 블록 전체를 렌더한다. 125개 노드와 166개 실제 연결, 기존 분기·복귀·승인 경로가 보존됐는지 대조한다. Markdown 뷰어는 ELK를 지원하는 Mermaid 버전이 필요하다.
